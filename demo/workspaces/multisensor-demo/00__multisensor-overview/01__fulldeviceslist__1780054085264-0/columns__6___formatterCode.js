if (value === null || value === undefined || value === '') { cellFormatter.customValue = '—'; } else { var n = Number(value); cellFormatter.customValue = isNaN(n) ? String(value) : n.toFixed(0) + ' %'; cellFormatter.style = 'text-align:right;'; if (!isNaN(n)) {   if (n < 20) { cellFormatter.style += 'color:#d62728;font-weight:bold;'; cellFormatter.leftIcon = 'fa-battery-empty'; }   else if (n < 50) { cellFormatter.leftIcon = 'fa-battery-quarter'; }   else if (n < 80) { cellFormatter.leftIcon = 'fa-battery-half'; }   else { cellFormatter.leftIcon = 'fa-battery-full'; } } }